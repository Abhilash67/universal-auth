// Main entry point for the universal-sdk package
import { createAuthProvider } from './auth-provider.js';

// Export the factory function to create auth providers
export default {
  createAuthProvider
};
